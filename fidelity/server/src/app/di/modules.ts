import type { Container } from 'inversify';

// Foundation placeholder for future module DI registration.
// Add module-specific bindings here as features migrate from server/core/.
export function registerApplicationModules(container: Container): Container {
  return container;
}

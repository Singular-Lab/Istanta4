# Imposta LF come line ending di default per tutti i file di testo
* text=auto

# Shell script e entrypoint Docker: forzare LF (mai CRLF) — su Linux CRLF causa errori di parsing
*.sh text eol=lf
docker/**/*.sh text eol=lf

# name: Common Workflow

# on:
#   workflow_call:
#     inputs:
#       node-version:
#         description: 'Node.js version to use'
#         required: true
#         type: string
#         default: '18'
#       os:
#         description: 'Operating system to run on'
#         required: true
#         type: string
#         default: 'ubuntu-latest'
#       cache-key:
#         description: 'Cache key for dependencies'
#         required: false
#         type: string
#         default: ''
#       steps:
#         description: 'Steps to execute (setup, install, test, build)'
#         required: true
#         type: string
#     secrets:
#       NPM_TOKEN:
#         required: false

# jobs:
#   common-job:
#     name: ${{ inputs.steps }} (Node ${{ inputs.node-version }} on ${{ inputs.os }})
#     runs-on: ${{ inputs.os }}

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Setup Node.js ${{ inputs.node-version }}
#         uses: actions/setup-node@v4
#         with:
#           node-version: ${{ inputs.node-version }}
#           cache: 'npm'
#           registry-url: 'https://registry.npmjs.org'

#       - name: Cache node_modules
#         uses: actions/cache@v4
#         with:
#           path: |
#             ~/.npm
#             node_modules
#             */*/node_modules
#           key: ${{ runner.os }}-node-${{ inputs.node-version }}-${{ inputs.cache-key }}-${{ hashFiles('**/package-lock.json') }}
#           restore-keys: |
#             ${{ runner.os }}-node-${{ inputs.node-version }}-${{ inputs.cache-key }}-

#       - name: Install dependencies
#         run: npm ci

#       # Setup step
#       - name: Setup environment
#         if: contains(inputs.steps, 'setup')
#         run: |
#           echo "Setting up environment..."
#           npm run models:check || true

#       # Test step
#       - name: Run tests
#         if: contains(inputs.steps, 'test')
#         run: |
#           echo "Running tests..."
#           npm run test:server:coverage

#       # Build step
#       - name: Build project
#         if: contains(inputs.steps, 'build')
#         run: |
#           echo "Building project..."
#           npm run build

#       # Quality checks step
#       - name: Quality checks
#         if: contains(inputs.steps, 'quality')
#         run: |
#           echo "Running quality checks..."
#           npm run type-check
#           ESLINT_USE_FLAT_CONFIG=true npm run lint
#           npm run security-check

#       # Upload artifacts if needed
#       - name: Upload coverage reports
#         if: contains(inputs.steps, 'test')
#         uses: actions/upload-artifact@v4
#         with:
#           name: coverage-${{ inputs.os }}-node-${{ inputs.node-version }}
#           path: coverage/
#           retention-days: 30

#       - name: Upload build artifacts
#         if: contains(inputs.steps, 'build')
#         uses: actions/upload-artifact@v4
#         with:
#           name: build-${{ inputs.os }}-node-${{ inputs.node-version }}
#           path: dist/
#           retention-days: 7

console.log("sono in Iagenzia js");

class IAgenzia {
    repartoPath = "";
    tipo_tracciato_selezionato = 0;
    Form_importazioneTracciato = "";
    BloccaScaricamentoListaAutomatico = false;

    compilaBoxAgenzia(item, callback) {
        console.error("Chiamata compila box agenzia non implementata");
        return null;
    }

    getForm_importazioneTracciato(callback) {
        console.error("Chiamata getForm_importazioneTracciato non implementata");
    }

    setRecord_revisioneArticolo(htmlItem) {
        console.error("Chiamata setRecord_revisioneArticolo non implementata");
    }

    filterRecordsTracciatiOnMenabo(lista) {
        console.error("Chiamata filterRecordsTracciatiOnMenabo non implementata");
    }

    ordinaRecordsTracciatoInMenabo(lista, cb) {
        console.error("Chiamata ordinaRecordsTracciatoInMenabo non implementata");
        cb(lista);
    }

    vuoiAggiungereFiltri(List) {
        console.error("Chiamata vuoiAggiungereFiltri non implementata");
    }

    onRenderRefInListaMenabo(groupElements, codice, area) {
        console.error("Chiamata onRenderRefInListaMenabo non implementata");
    }

    onRenderFinestraEsportazionePoP() {
        console.error("Chiamata onRenderFinestraEsportazionePoP non implementata");
    }

    AggiungiInfoPezzo(groupElements, codice, area) {
        console.error("Chiamata AggiungiInfoPezzo non implementata");
    }

    //ApplicaSegnaposto(groupElements, codice) {
    //    console.error("Chiamata ApplicaSegnaposto non implementata");
    //}

    findElementFromBottomUp(startElement, targetSelector) {
        console.error("Chiamata findElementFromBottomUp non implementata");
    }

    findElementFromUpBottom(startElement, targetSelector) {
        console.error("Chiamata findElementFromUpBottom non implementata");
    }

    AggiornaSegnapostiPostRicerca() {
        console.error("Chiamata AggiornaSegnapostiPostRicerca non implementata");
    }

    contaSiblingSottoSegnaposto(segnaposto) {
        console.error("Chiamata contaSiblingSottoSegnaposto non implementata");
    }

    getForm_ricercaTracciato() {
        console.error("Chiamata getForm_ricercaTracciato non implementata");
        return null;
    }

    customViewOnRecordSyncFromIndd(dato, codice_gruppo, elementGruppo, elementiFigli) {
        console.error("Chiamata customViewOnRecordSyncFromIndd non implementata");
    }

    returnParametriCodiciScatto() {
        console.error("Chiamata returnParametriCodiciScatto non implementata");
    }

    NascondiParametriInserimentoArticoli() {
        console.error("Chiamata NascondiParametriInserimentoArticoli non implementata");
    }

    OperazioniInserimentoArticoliCustom(idTracciato) {
        console.error("Chiamata OperazioniInserimentoArticoliCustom non implementata");
    }

    getMeccanicaCustom(idMastro, note) {
        console.warn("Chiamata getMeccanicaCustom non implementata");
    }

    aggiungiDettagliARecordInTracciato(groupElements, codice) {
        console.warn("Chiamata aggiungiDettagliARecordInTracciato non implementata");
    }

    bindSchemaOrdinamento(cb) {
        console.error("Chiamata bindSchemaOrdinamento non implementata");
    }

    bindSchemaOrdinamentoClassificazioni() {
        console.warn("Chiamata bindSchemaOrdinamentoClassificazioni non implementata");
    }

    assegnaPostiOrdinamento(sender) {
        console.warn("Chiamata assegnaPostiOrdinamento non implementata");
    }

    cambioAreaOrdinamento(sender, inx) {
        console.warn("Chiamata cambioAreaOrdinamento non implementata");
    }

    cambioSettoriOrdinamento(sender, inx) {
        console.warn("Chiamata cambioSettoriOrdinamento non implementata");
    }

    cambioGruppiOrdinamento(sender, inx) {
        console.warn("Chiamata cambioGruppiOrdinamento non implementata");
    }

    cambiaOrdinamentoAreaVal(sender) {
        console.warn("Chiamata cambiaOrdinamentoAreaVal non implementata");
    }

    cambiaOrdinamentoSettoreVal(sender) {
        console.warn("Chiamata cambiaOrdinamentoSettoreVal non implementata");
    }

    cambiaOrdinamentoGruppoVal(sender) {
        console.warn("Chiamata cambiaOrdinamentoGruppoVal non implementata");
    }

    cambiaOrdinamentoSegmentoVal(sender) {
        console.warn("Chiamata cambiaOrdinamentoSegmentoVal non implementata");
    }

    cambioCategoria(sender, type) {
        console.warn("Chiamata cambioCategoria non implementata");
    }

    resetOrdinamento() {
        console.warn("Chiamata resetOrdinamento non implementata");
    }

    salvaSchemaOrdinamento() {
        console.warn("Chiamata salvaSchemaOrdinamento non implementata");
    }

    ordinamentoRecursiveRead(action, _temp) {
        console.error("Chiamata salvaSchemaOrdinamento non implementata");
    }

    SetPrezzi(htmlItem, inTrac, areaTracciato, groupItem) {
        console.warn("Chiamata salvaSchemaOrdinamento non implementata");
    }

    AggiungiInfo(groupElements, codice) {
        console.warn("Chiamata AggiungiInfo non implementata");
    }

    getCustomConfronti() {
        console.error("Chiamata getCustomConfronti non implementata");
    }

    controllaCambioPromoCustomConfrontiPostOperation(tendinaVal) {
        console.warn("Chiamata controllaCambioPromoCustomConfrontiPostOperation non implementata");
    }

    appendPromoToOtherElements(result) {
        console.warn("Chiamata appendPromoToOtherElements non implementata");
    }

    impostaCheckbox(idPromo) {
        console.warn("Chiamata impostaCheckbox non implementata");
    }

    confrontaLocandine() {
        console.warn("Chiamata confrontaLocandine non implementata");
    }

    setScheda(schedaNumber, testata) {
        console.warn("Chiamata setScheda non implementata");
    }

    checkCustomErrors(ref, page) {
        console.warn("Chiamata checkCustomErrors non implementata");
    }

    compilaBoxConInformazioniCustom(htmlItem, dataItem) {
        console.warn("Chiamata compilaBoxConInformazioniCustom non implementata");
    }

    aggiungiAreeInAreeOutDettaglio(htmlItem, dataItem) {
        console.warn("Chiamata aggiungiAreeInAreeOutDettaglio non implementata");
    }

    confrontiSetTipoMateriali(optionSelected) {
        console.warn("Chiamata confrontiSetTipoMateriali non implementata");
    }

    listen() {
        console.warn("Chiamata listen non implementata");
    }

    aggiungiSezioneOrdinamento(sender) {
        console.warn("Chiamata aggiungiSezioneOrdinamento non implementata");
    }

    aggiungiCampiRevisione(htmlItem, dataItem) {
        console.warn("Chiamata aggiungiCampiRevisione non implementata");
    }

    getParametriSegnaposto() {
        console.warn("Chiamata getParametriSegnaposto non implementata");
    }

    customSegnaposti() {
        console.warn("Chiamata customSegnaposti non implementata");

    }

    filterRecordsTracciatiOnMenabo(data) {
        console.warn("Chiamata filterRecordsTracciatiOnMenabo non implementata");
        return data;
    }

    //Chiamata da parte del core per sapere se AGENZIA necessita di forzare auto revisione da lista. Opzione FALSE di default (**ARENA necessita di questa opzione a TRUE)
    isAutoRvisioneInImportazioneEnabled() {
        return false;
    }

    modificaColPesoTracciato(colPesoTracciato, pesoTracciatoVal, inTrac, group) {
        console.warn("Chiamata modificaColGruppo non implementata");
    }

    mostraFirmaTracciato() {
        console.warn("Chiamata mostraFirmaTracciato non implementata");
        return false;
    }

    customCampiEtichettaturaTracciato() {
        console.warn("Chiamata customCampiEtichettaturaTracciato non implementata");
        return [];
    }

    customCampiEtichettaturaPromo() {
        console.warn("Chiamata customCampiEtichettaturaPromo non implementata");
        return [];
    }

    customCampiEtichettaturaPromoTracciati() {
        console.warn("Chiamata customCampiEtichettaturaPromoTracciati non implementata");
        return [];
    }

    nascondiCampiIndesideratiRevisore(html) {
        console.warn("Chiamata nascondiCampiIndesideratiRevisore non implementata");
        return;
    }

    controlChangeTextCustom(box, ref) {
        console.warn("Chiamata controlChangeTextCustom non implementata");
        return;
    }

    nascondiFiltroTracciatiInSyncFoto() {
        console.warn("Chiamata nascondiFiltroTracciato non implementata");
        return false;
    }
}